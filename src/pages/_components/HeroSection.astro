---
import Keyboard from '../_components/Keyboard.astro'
---

<section
  class='flex flex-col gap-8 pb-32 pt-16 sm:min-h-screen sm:justify-center'
>
  <div
    class:list={[
      'flex w-fit items-center rounded-full',
      'gap-3 py-1.5 pl-3 pr-4',
      'border border-slate-600/75',
      'bg-slate-950/25'
    ]}
  >
    <div class='relative size-3'>
      <div
        class='absolute size-full animate-ping rounded-full bg-green-300 opacity-65'
      >
      </div>
      <div class='drop-shadow-green size-full rounded-full bg-green-400'></div>
    </div>
    <h3 class='text-sm text-slate-200 max-xs:text-xs'>
      Available for projects
    </h3>
  </div>
  <h1
    class='font-geist text-[clamp(2rem,5vw,3rem)] font-semibold leading-none tracking-[-0.04em] sm:font-medium'
  >
    A professional typer with a
    <br class='max-xs:hidden' />passion for designing & building
  </h1>
  <h2 class='mb-8 max-w-2xl leading-relaxed text-slate-400 md:text-lg'>
    Hi, I'm Jesica, a software engineer based in Jakarta, Indonesia. From Figma
    to TypeScript, I create seamless experiences for web and mobile.
  </h2>
  <Keyboard />
</section>

<style is:global>
  .drop-shadow-green {
    filter: drop-shadow(0px 0px 8px #59ff7e);
  }

  @keyframes pulse-glow {
    0%,
    100% {
      transform: scale(1);
      box-shadow: 0 0 0 0 rgba(74, 222, 128, 0.5);
    }
    50% {
      transform: scale(1.05);
      box-shadow: 0 0 0 6px rgba(74, 222, 128, 0);
    }
  }

  .animate-pulse-glow {
    animation: pulse-glow 2s infinite;
  }

  .text-gradient {
    background: rgb(165, 238, 209);
    background: linear-gradient(
      90deg,
      rgba(165, 238, 209, 1) 0%,
      rgba(239, 255, 193, 1) 100%
    );
    background-clip: text;
  }
</style>
