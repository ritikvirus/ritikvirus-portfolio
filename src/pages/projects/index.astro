---
import { Suitcase } from '@icons/Suitcase'

import ProjectCard from '@/components/ProjectCard'
import RayLight from '@/components/Raylight'
import BaseLayout from '@/layouts/BaseLayout.astro'
import { cn } from '@/lib/utils'
---

<BaseLayout>
  <RayLight.Container class='-z-10' slot='decorator'>
    <RayLight.Component
      className={cn(
        'h-[400px] w-[clamp(400px,100vw,1260px)] rotate-[42deg] opacity-60',
        '-right-[5%] -top-[36%] left-auto sm:-top-[18%]'
      )}
    />
    <RayLight.Component className='-top-32 left-[35%] opacity-25' />
    <RayLight.Component
      className='left-[45%] top-[5%] h-[180px] opacity-25 max-lg:hidden'
    />
  </RayLight.Container>
  <section class='mt-16 space-y-8 sm:mt-36'>
    <div
      class='flex w-fit items-center gap-2 rounded-full bg-[#162725] px-4 py-2 text-emerald-100'
    >
      <Suitcase />
      <h1 class='text-sm font-medium max-sm:text-xs'>Projects</h1>
    </div>
    <h2
      class='text-[clamp(32px,7vw,52px)] font-bold leading-none tracking-tight'
    >
      Milestones in the <br /> learning journey
    </h2>
    <h3 class='tracking-wider text-gray-400 max-md:text-sm'>
      Each project marks a step forward, showcasing my growth and journey as a
      developer. <br class='max-md:hidden' /> Explore how I’ve tackled challenges
      and built solutions along the way.
    </h3>
  </section>

  <section class='mt-12 grid gap-12 sm:grid-cols-2 lg:grid-cols-3'>
    <ProjectCard
      href='/projects/bookmarked'
      title='✨ Bookmarked: Your Go-To Tool for Curating Tweets in Notion'
      year='2024'
      description='A full-stack application that enables users to save tweets to Notion via Telegram bot.'
    />
  </section>
</BaseLayout>
